# Application
APP_ENV=development
PORT=3002
LOG_LEVEL=debug

# RabbitMQ (required)
RABBITMQ_URL=amqp://golrox:password@localhost:5672/
RABBITMQ_QUEUE=golrox.realtime

# RabbitMQ Exchanges (separated by feature)
# These must match the exchanges used by golroblox-admin publisher
RABBITMQ_EXCHANGE_ORDERS=golrox.events.orders
RABBITMQ_EXCHANGE_PAYMENTS=golrox.events.payments
RABBITMQ_EXCHANGE_NOTIFICATIONS=golrox.events.notifications

# JWT (optional - if empty, only anonymous connections allowed)
JWT_SECRET=your-jwt-secret-here

# CORS (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
